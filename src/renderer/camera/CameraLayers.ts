/** Layer 0 — world scenery + cockpit interior. */
export const COCKPIT_VIEW_LAYER = 0
/** Layer 1 — player external placeholder mesh (excluded from cockpit camera). */
export const PLAYER_EXTERNAL_LAYER = 1
